using System;
using System.Collections.Generic;
using System.Text;
using System.IO;

namespace VmManagerConfiguration
{
    class LocationStringConverter
    {

        public String toRegString(string fileUrl)
        {
            try
            {
                return "";
            }
            catch (Exception e) {
                return "";
            }

        }

        public String toFileString(string fileUrl)
        {
            
            try
            {
              //  string[] temp = fileUrl.Substring(1, fileUrl.Length - 2);//.Split('\\');
                //tring returnString = 
                    //Path.GetFullPath(temp);
               // foreach (string s in temp)
             //   {
                   // returnString += ""+s+"\";
              //  }
                

                return "";
            }
            catch (Exception e)
            {
                return "";
            }

        }
    }
}
